<!--     $Author: woo $    $Date: 2016/03/17 15:29:26 $    $Revision: 1.2 $    $Source: /Users/woo/cvsrep/queue/queue_entry.page,v $    Typical page that will be read in and replace the text in the page div     The query on the calling page retrieved the data and stored it in 'data' var.     Here that data is used to show that it was retrieved.  We should really     call a servlet or web service to get the actual process, job step and the      layout to display here using another page load.--><script type="text/javascript">        var uName;        var uRole;        var uOrg;    </script>        <h3>Typical page that will be read in and replace the text in the page div     The query on the calling page retrieved the data and stored it in 'data' var.     Here that data is used to show that it was retrieved.  We should really     call a servlet or web service to get the actual process, job step and the      layout to display here using another page load.</h3>     <br>            <div id="entry">Replace with entry number</div>            <div id="from">Replace with from</div>            <div id="msg">Replace with msg</div>    <script type="text/javascript">        function getEntry() {            $("#entry").text("Entry: "+ data.ENTRY_NUMBER);            $("#from").text("From: "+ data.OWNER);            $("#msg").text("Message: "+ data.COMMENT);        };            $("#myTitle").text("Queue Entry");    	    getEntry();    </script>
